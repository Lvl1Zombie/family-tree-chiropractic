const root=document.documentElement;addEventListener('pointermove',e=>{root.style.setProperty('--mx',`${(e.clientX/innerWidth-.5)*-1.4}%`);root.style.setProperty('--my',`${(e.clientY/innerHeight-.5)*-1.4}%`)});
const notes={1:['Photocyte membrane','A translucent dermal layer that releases stored photons in waves shorter than a human breath.','82%'],2:['Luciferin chamber','A microscopic reservoir where oxygen and luciferin meet without producing heat.','64%'],3:['Signal filament','A responsive nerve structure that synchronizes the whole colony in under nine milliseconds.','91%']};document.querySelectorAll('.pin').forEach((b,i)=>b.onclick=()=>{const n=notes[i+1];document.querySelector('#note').textContent=n[0];document.querySelector('#desc').textContent=n[1];document.querySelector('.meter span').style.setProperty('--v',n[2])});
addEventListener('scroll',()=>{const a=document.querySelector('.anatomy').getBoundingClientRect();const p=Math.max(0,Math.min(1,-a.top/(a.height-innerHeight)));root.style.setProperty('--scale',1.05+p*.18)},{passive:true});
const c=document.querySelector('#spores'),x=c.getContext('2d');let ps=[];function fit(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);ps=Array.from({length:Math.min(100,innerWidth/10)},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:.3+Math.random()*1.5,v:.1+Math.random()*.35,a:.1+Math.random()*.5}))}function loop(){x.clearRect(0,0,innerWidth,innerHeight);x.fillStyle='#76ffe9';ps.forEach(p=>{p.y-=p.v;p.x+=Math.sin(p.y*.01)*.08;if(p.y<0)p.y=innerHeight;x.globalAlpha=p.a;x.beginPath();x.arc(p.x,p.y,p.r,0,7);x.fill()});requestAnimationFrame(loop)}fit();addEventListener('resize',fit);if(!matchMedia('(prefers-reduced-motion: reduce)').matches)loop();

const lumenSections=[...document.querySelectorAll('section:not(.hero)')];const lumenReveal=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('in')),{threshold:.12});lumenSections.forEach(s=>{s.classList.add('reveal');lumenReveal.observe(s)});

const lumenVideo=document.createElement('video');lumenVideo.src='../assets/lumen.mp4';lumenVideo.poster='../assets/lumen.webp';lumenVideo.autoplay=true;lumenVideo.muted=true;lumenVideo.loop=true;lumenVideo.playsInline=true;lumenVideo.preload='metadata';lumenVideo.setAttribute('aria-hidden','true');document.querySelector('.hero-image').prepend(lumenVideo);if(matchMedia('(prefers-reduced-motion: reduce)').matches){lumenVideo.autoplay=false;lumenVideo.pause()}
